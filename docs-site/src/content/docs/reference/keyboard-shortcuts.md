---
title: Keyboard shortcuts
description: Every default Remarc shortcut in one place, covering global hotkeys, comment composer keys, and Chrome extension bindings.
---

Remarc's global shortcuts default to `Ctrl+Option` plus a letter. Rebind any of them in Settings > Shortcuts, which warns you when a new binding conflicts with another Remarc shortcut.

## Global shortcuts

Global shortcuts work in any app while Remarc is running.

| Action | Shortcut |
| --- | --- |
| Open Remarc | `Ctrl+Option+R` |
| Comment on selection | `Ctrl+Option+C` |
| Screenshot | `Ctrl+Option+S` |
| Screenshot & Send Instantly | Unassigned |
| Paste All (copy all comments) | `Ctrl+Option+P` |
| Start Crit Mode | `Ctrl+Option+M` |
| Voice Input | `Ctrl+Option+V` |
| Dictation (push to talk) | `Ctrl+Option+D` |
| Dictation (hands-free) | `Ctrl+Option+H` |
| Paste Last Dictation | `Ctrl+Option+L` |

:::note
Voice Input, Dictation, Paste Last Dictation, and Crit Mode require macOS 26 (Tahoe) or later. On older versions the Voice Input, Dictation, and Paste Last Dictation rows are hidden in Settings.
:::

**Screenshot & Send Instantly** ships unassigned. Its row appears in Settings > Shortcuts only after you enable **Allow comments to wake paired agent sessions** in Settings > MCP Integrations.

Either dictation shortcut, push to talk or hands-free, can be bound to the fn/globe key instead of a key combination in Settings > Voice. Only one of the two can use the fn key at a time.

Hands-free [dictation](/voice/dictation/) starts with a single tap of the push-to-talk shortcut by default; `Ctrl+Option+H` also starts it directly. If you want a different binding, set Hands-free mode to Custom shortcut in Settings > Voice, where its recorder appears.

## Comment composer

The comment composer responds to two keys of its own.

| Action | Shortcut |
| --- | --- |
| Save the comment | `Cmd+Return` |
| Cancel and close | `Escape` |

## Chrome extension

The [Chrome extension](/chrome-extension/)'s capture shortcuts work inside the browser and can be changed in Settings > Chrome Extension or Settings > Shortcuts.

| Action | Shortcut |
| --- | --- |
| Grab Element | `Option+Shift+G` |
| Select Region | `Option+Shift+R` |

Chrome only accepts `Ctrl+Shift` or `Option+Shift` combinations; `Cmd` is not supported.
